# 趣灵 aha-flash — 当前开发 Backlog

> 文档定位：这是临时迭代入口，只保留未完成任务。完成后必须合并到长期文档并归档到 `CHANGELOG.md`。不要在这里长期保存 PRD、架构说明或已完成验收记录。

---

## 使用规则

1. 用户可以把新规划放进 `BACKLOG.md` 或其他临时文档。
2. Codex 开发时优先按最新规划文档执行。
3. 任务完成后：
   - 完成记录写入 `CHANGELOG.md`
   - 长期产品变化写入 `PRD.md`
   - 长期技术变化写入 `ARCHITECTURE.md`
   - 实施状态写入 `IMPLEMENTATION.md`
   - 从 `BACKLOG.md` 移除已完成任务
4. 新文档与旧文档重复时，不复制重复内容，只提炼新增任务和差异。

---

## 当前优先级

当前规划文档中的开发任务已完成。下一轮请放入新的 `BACKLOG.md`、`ROADMAP.md` 或其他临时规划文档。

| 优先级 | 任务 | 原因 |
|---|---|---|
| - | 暂无 | 等待新的规划输入 |

---

## 人工任务

| ID | 任务 | 说明 |
|---|---|---|
| H01 | 配置真实 DeepSeek API Key | 写入 Vercel 环境变量，不提交到仓库 |
| H02 | 选择生产状态存储 | Vercel KV、Postgres、Redis 或其他方案 |
| H03 | 扩充隐喻域映射表 | 摄影、钓鱼、健身、烘焙等 |
| H04 | 真实用户测试 | 记录隐喻相关性、交互完成率、卡点 |
| H05 | 配置搜索 API Key | 优先写入 `BRAVE_SEARCH_API_KEY`，也可配置 Google 或 Tavily 备用 |

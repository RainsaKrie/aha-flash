# 趣灵 aha-flash — 产品需求文档

> 文档定位：只记录长期产品真相。临时任务、实现拆解、验收记录不放在 PRD；完成后归档到 `CHANGELOG.md`，开发路线放在 `IMPLEMENTATION.md`。

---

## 1. 产品定位

趣灵（aha-flash）是一款 AI 原生交互式知识学习引擎。

一句话：把复杂概念和长篇信息编译成可互动的小组件，让用户通过操作、反馈和顿悟来理解知识。

核心差异：
- 不以长篇解释为主要输出，而以 Generative UI 为主要输出。
- 不只解释“是什么”，还让用户在交互中看到机制如何运转。
- 不使用通用教学模板，而是结合用户状态、兴趣和知识盲区生成个性化隐喻。

---

## 2. 目标用户

| 用户画像 | 核心痛点 | 典型场景 |
|---|---|---|
| 非专业背景学习者 | 专业术语门槛高，长文难读 | 快速理解金融、科技、商业概念 |
| 职场学习者 | 时间碎片化，需要快速建立认知 | 通勤或工作间隙学习一个复杂概念 |
| 知识爱好者 | 信息源很多，但消化成本高 | 把长文、视频、播客变成可玩的理解路径 |
| 年轻用户 | 传统学习形式枯燥 | 像玩小游戏一样理解抽象知识 |

---

## 3. Magic Moment

用户输入：

```text
期权是什么？用我能听懂的方式讲。
```

系统读取用户状态：
- 背景：非金融专业
- 爱好：抽卡游戏
- 知识盲区：金融基础
- 隐喻偏好：游戏机制

系统输出一个可交互组件，例如“期权抽卡模拟器”：
- 用户先支付一小笔“期权费”
- 再观察未来结果
- 如果高价值结果出现，可以按锁定价行动
- 如果没有出现，最大损失只等于期权费

用户通过一次点击和反馈理解：

```text
期权 = 未来选择权 + 有限损失 + 上涨收益暴露
```

---

## 4. 核心能力

### F1. Stateful Memory

系统维护轻量用户状态，记录：
- 背景
- 爱好
- 知识盲区
- 隐喻偏好
- 已学概念资产
- 最近学习主题
- 关键洞察
- 理解深度

原则：
- V1 使用匿名用户 ID + localStorage。
- 不保存全量对话原文，只保存压缩状态。
- 已学概念以轻量资产记录，只保留概念名、Pattern、Template、理解深度和主题域。
- 用户可以在知识沙盒中按主题回看已学概念地图。
- 状态目标大小保持在 5KB 左右。
- 生产持久化最终应迁移到数据库或 KV；本地文件和 Vercel `/tmp` 只作为 demo 阶段方案。

### F2. Intent And State Reflection

每轮对话先判断用户意图：
- `knowledge`：解释、对比、测验、学习概念
- `preference`：表达背景、爱好、偏好、希望换一种讲法
- `casual`：与学习无关的闲聊

每轮结束后提炼：
- 用户是否透露了新背景或偏好
- 当前概念理解深度
- 可写入状态的摘要

### F3. Personalized Metaphor

系统根据用户状态选择隐喻域，例如：

| 用户兴趣 | 隐喻域 | 适配概念 |
|---|---|---|
| 抽卡游戏 | 概率、锁价、资源投入 | 期权、保险、投资组合 |
| 赛车 | 速度、策略、进站 | 算法复杂度、竞争策略 |
| 音乐 | 节奏、和弦、结构 | 数学、编程、系统关系 |
| 烹饪 | 配方、火候、比例 | 化学、系统架构 |

### F4. Generative UI

LLM 输出结构化 UI Schema，前端按 `pattern + template + payload` 渲染互动组件。

Schema 可附带 `next_concepts`，用于在每次学习后推荐 1-2 个相邻概念，并支持点击后直接进入下一轮学习。

当前已支持的交互模式：

| Pattern | 用途 | 当前模板 |
|---|---|---|
| `probability` | 概率、期权、保险 | `card_flip_reveal`, `spin_wheel` |
| `parameter_explore` | 参数影响、因果变量 | `single_slider`, `dual_slider` |
| `concept_memory` | 术语记忆、概念映射 | `term_cards`, `grid_match` |
| `process_timeline` | 历史、流程、演化 | `horizontal_timeline`, `vertical_scroll` |
| `comparison` | 对比、辨析、权衡 | `split_panel`, `overlay_fade` |
| `knowledge_check` | 测验、理解检查 | `single_question`, `combo_chain` |
| `system_builder` | 系统、模块、架构 | `module_sandbox`, `flow_connect` |
| `narrative_branch` | 案例、决策、逻辑谬误 | `branch_story` |
| `classification_sort` | 分类、归因、边界判断 | `category_buckets` |
| `simulation_play` | 复利、供需、网络效应 | `parameter_simulation` |

### F5. Source Ingestion

V1 支持：
- 用户直接输入文本
- 用户粘贴网页链接
- 用户粘贴 YouTube 链接
- 学习完成后的知识链推荐
- `/sandbox` 知识沙盒回看已学概念

计划支持：
- 搜索工具
- 播客源
- 论文/长文摘要

---

## 5. 非功能需求

| 需求 | 指标 |
|---|---|
| 首屏加载 | 目标 < 2s |
| 组件生成 | 目标 < 10s |
| Schema 稳定性 | 非法输出可修复或 fallback |
| 移动端 | 核心交互可用 |
| 可访问性 | 交互控件有语义标签 |
| 状态体积 | 轻量、可压缩、可迁移 |

---

## 6. V1 不做

- 用户注册/登录
- 多设备同步
- 付费订阅
- 社交系统
- 完整内容审核后台
- 离线模式
- 生产级长期记忆数据库

---

## 7. 成功指标

| 指标 | 目标 |
|---|---|
| 从输入到看到组件 | < 10s |
| 交互完成率 | > 70% |
| 隐喻相关性用户自评 | > 85% |
| 状态偏好匹配满意度 | > 80% |
| 传统长文本解释占比 | 尽量低，优先 UI |

---

## 8. 文档关系

| 文档 | 用途 |
|---|---|
| `PRD.md` | 产品目标、用户价值、长期功能边界 |
| `ARCHITECTURE.md` | 当前技术架构和关键决策 |
| `IMPLEMENTATION.md` | 当前实现状态和后续路线 |
| `BACKLOG.md` | 临时迭代输入，只保留未完成任务 |
| `CHANGELOG.md` | 已完成任务和验收归档 |
